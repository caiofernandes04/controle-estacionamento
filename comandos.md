php artisan serve --host=0.0.0.0 --port=8000

git status
git add .
git commit -m "Descrição das alterações"
git push origin main

git checkout -b feature/login - branch nova
git branch - branch atual
